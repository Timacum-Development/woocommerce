import styled from 'styled-components';
import { color } from '../../styles';

export const SmallImg = styled.img`
	display: block;
	border-radius: 10px;
	position: absolute;
	box-shadow: 0px 18px 53px 0 rgba(215, 228, 249, 0.5);
	z-index: 10;
`;
